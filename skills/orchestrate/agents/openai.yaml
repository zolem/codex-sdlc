interface:
  display_name: "z:orchestrate"
  short_description: "Run a review-gated software delivery pipeline"
  default_prompt: "Use $orchestrate to deliver this feature from brief through verified implementation."

policy:
  allow_implicit_invocation: false
