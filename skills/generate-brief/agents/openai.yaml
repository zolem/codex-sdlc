interface:
  display_name: "z:generate-brief"
  short_description: "Turn a product idea into an actionable brief"
  default_prompt: "Use $generate-brief to turn my product idea into a structured implementation brief."

policy:
  allow_implicit_invocation: false
